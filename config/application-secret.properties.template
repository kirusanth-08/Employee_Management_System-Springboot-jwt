# This is a template file for application secrets
# Copy this file to application-secret.properties and fill in your actual values
# The application-secret.properties file is excluded from git by .gitignore

# JWT Configuration
app.jwt-secret=your_secure_jwt_secret_key_at_least_256_bits
app.jwt-expiration-milliseconds=604800000

# Database credentials (optional, can override main properties)
# spring.datasource.username=your_db_username
# spring.datasource.password=your_db_password
